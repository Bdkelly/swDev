version: 0.0
os: linux
files:
  - source: /static
    destination: /var/www/html/
hooks:
  BeforeInstall:
    - location: startup/install_dependencies
      timeout: 300
      runas: root
    - location: scripts/start_server
      timeout: 300
      runas: root